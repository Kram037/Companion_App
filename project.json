{
  "$schema": "./node_modules/nx/schemas/project-schema.json",
  "name": "companion-app",
  "projectType": "application",
  "sourceRoot": ".",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/serve-static.mjs . 8000"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/companion-app"],
      "options": {
        "command": "node tools/build-static.mjs dist/apps/companion-app"
      }
    },
    "vite-serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run vite:dev"
      }
    },
    "vite-build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/vite/companion-app"],
      "options": {
        "command": "npm run vite:build"
      }
    },
    "check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run typecheck && node tools/check-js.mjs && node tools/check-data-boundary.mjs && node tools/check-index-bootstrap.mjs && node tools/check-react-boundaries.mjs"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run test"
      }
    },
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run e2e"
      }
    }
  }
}
