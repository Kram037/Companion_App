{
  "name": "companion-app-workspace",
  "private": true,
  "scripts": {
    "nx": "nx",
    "serve": "nx run companion-app:serve",
    "build": "nx run companion-app:build",
    "check": "nx run companion-app:check"
  },
  "devDependencies": {
    "nx": "^21.0.0"
  }
}
